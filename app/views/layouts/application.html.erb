<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Memories App" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Memories App" %>">
    <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
    <%= stylesheet_link_tag "application", 'https://fonts.googleapis.com/css?family=Bad+Script', :media => "all" %> 
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>

 
  </head>
  <body>
    <header>
      <%= render 'layouts/navigation' %>
    </header>
    <br>
    <br>
    <br>
    <br>
    <br>
    <br>
    <br>
    <div class="container">
    <main role="main">
       <%= render 'layouts/messages' %>
       <%= yield %>
       </div>
    </main>
  </body>
  <footer class="navbar-fixed-bottom"></footer>
</html>
